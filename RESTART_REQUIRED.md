# ⚠️ إعادة تشغيل مطلوبة!

## المشكلة
السيرفر لا يزال يستخدم الإعدادات القديمة من ملف `.env`.

## الحل

### الخطوة 1: أوقف السيرفر
اضغط `Ctrl + C` في Terminal لإيقاف السيرفر.

### الخطوة 2: تحقق من ملف `.env`
تأكد من أن الملف يحتوي على:

```env
API_URL=https://www.aldorer.com/api
GEMINI_API_KEY=AIzaSyD68Gr85NC1A33pbb8qc8iWrM2veySMJnU4
GEMINI_CHAT_MODEL=gemini-1.5-flash
```

### الخطوة 3: أعد تشغيل السيرفر

```bash
pnpm dev
```

### الخطوة 4: اختبر الشات بوت
1. افتح http://localhost:3000
2. انقر على زر الشات
3. اكتب: "مرحبا"
4. يجب أن يرد بنجاح! 🎉

## لماذا إعادة التشغيل مطلوبة؟

Nuxt يقرأ متغيرات البيئة (`.env`) عند بدء التشغيل فقط. أي تغيير في `.env` يتطلب إعادة تشغيل السيرفر.

## التحقق من النموذج المستخدم

بعد إعادة التشغيل، يجب أن ترى في console:

```
✓ Nuxt Nitro server built in XXXms
```

وعند إرسال رسالة، يجب أن يستخدم `gemini-1.5-flash` وليس `gemini-2.0-flash-exp`.

## إذا استمرت المشكلة

### الحل 1: امسح الـ cache
```bash
# أوقف السيرفر
# ثم امسح الـ cache
rm -rf .nuxt
rm -rf node_modules/.cache

# أعد التشغيل
pnpm dev
```

### الحل 2: تحقق من المفتاح
تأكد من أن `GEMINI_API_KEY` كامل وصحيح:
```
AIzaSyD68Gr85NC1A33pbb8qc8iWrM2veySMJnU4
```

### الحل 3: انتظر دقيقة
إذا كنت قد استخدمت النموذج التجريبي كثيرًا، انتظر 1-2 دقيقة قبل المحاولة مرة أخرى.

---

**ملاحظة مهمة:** 
بعد أي تغيير في ملف `.env`، يجب **دائمًا** إعادة تشغيل السيرفر! 🔄
