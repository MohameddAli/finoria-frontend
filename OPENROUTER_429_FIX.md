# إصلاح خطأ 429 في OpenRouter

## المشكلة 🔴

```
{error: true, statusCode: 429, message: "Provider returned error"}
```

## السبب 🤔

خطأ 429 من OpenRouter يعني أحد الأمور التالية:

### 1. النموذج المجاني مشغول
- `deepseek/deepseek-chat-v3-0324:free` قد يكون مشغولاً جدًا
- النماذج المجانية لها حدود على عدد المستخدمين المتزامنين

### 2. مفتاح API غير صحيح
- المفتاح قد يكون منتهي الصلاحية
- المفتاح قد يكون محظورًا

### 3. الحساب يحتاج تفعيل
- قد تحتاج إلى تسجيل الدخول وتفعيل الحساب

## الحل ✅

### الحل 1: تغيير النموذج (موصى به)

جرب نماذج مجانية بديلة في `.env`:

```env
# جرب Llama 3.2 (عادة أكثر استقرارًا)
OPENROUTER_MODEL=meta-llama/llama-3.2-3b-instruct:free

# أو Gemma 2
OPENROUTER_MODEL=google/gemma-2-9b-it:free

# أو Phi-3
OPENROUTER_MODEL=microsoft/phi-3-mini-128k-instruct:free

# أو Qwen 2
OPENROUTER_MODEL=qwen/qwen-2-7b-instruct:free
```

### الحل 2: احصل على مفتاح API جديد

1. اذهب إلى: https://openrouter.ai/keys
2. احذف المفتاح القديم
3. أنشئ مفتاح جديد
4. ضعه في `.env`:

```env
OPENROUTER_API_KEY=sk-or-v1-your-new-key-here
```

### الحل 3: تحقق من حسابك

1. اذهب إلى: https://openrouter.ai/account
2. تأكد من أن الحساب مفعّل
3. تحقق من الرصيد (يجب أن يكون $0 للنماذج المجانية)

### الحل 4: انتظر قليلاً

إذا كان النموذج مشغولاً:
- انتظر 1-2 دقيقة
- حاول مرة أخرى

## 🚀 الحل السريع

### الخطوة 1: غيّر النموذج في `.env`

```env
API_URL=https://www.aldorer.com/api
OPENROUTER_API_KEY=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
OPENROUTER_MODEL=meta-llama/llama-3.2-3b-instruct:free
```

### الخطوة 2: أعد تشغيل السيرفر

```bash
# أوقف السيرفر
Ctrl + C

# أعد التشغيل
pnpm dev
```

### الخطوة 3: اختبر

1. افتح http://localhost:3000
2. انقر على زر الشات
3. اكتب: "مرحبا"
4. يجب أن يعمل الآن! 🎉

## 📊 النماذج المجانية الموصى بها

### 1. Meta Llama 3.2 ⭐ (الأكثر استقرارًا)
```env
OPENROUTER_MODEL=meta-llama/llama-3.2-3b-instruct:free
```
- **الاستقرار:** ممتاز
- **السرعة:** سريع
- **العربية:** جيد جدًا
- **التوفر:** عالي

### 2. Google Gemma 2
```env
OPENROUTER_MODEL=google/gemma-2-9b-it:free
```
- **الاستقرار:** جيد
- **السرعة:** متوسط
- **العربية:** جيد
- **التوفر:** جيد

### 3. Microsoft Phi-3
```env
OPENROUTER_MODEL=microsoft/phi-3-mini-128k-instruct:free
```
- **الاستقرار:** جيد
- **السرعة:** سريع جدًا
- **العربية:** متوسط
- **التوفر:** جيد

### 4. Qwen 2
```env
OPENROUTER_MODEL=qwen/qwen-2-7b-instruct:free
```
- **الاستقرار:** جيد
- **السرعة:** سريع
- **العربية:** ممتاز
- **التوفر:** متوسط

## 🔧 التحسينات المطبقة

### 1. معالجة أخطاء محسّنة
تم تحديث `server/api/chat/openrouter.post.ts` لعرض رسائل خطأ أوضح:

```typescript
if (response.status === 429) {
  throw createError({
    statusCode: 429,
    message: 'Rate limit exceeded. Please try again in a moment.',
  });
}
```

### 2. تسجيل تفاصيل الأخطاء
الآن يتم تسجيل تفاصيل كاملة عن الخطأ في console:

```typescript
console.error('OpenRouter API Error Details:', {
  status: response.status,
  errorData: errorData,
  model: modelName,
});
```

### 3. نماذج بديلة
تم إضافة قائمة بالنماذج المجانية البديلة في الكود.

## ⚠️ ملاحظات مهمة

### ✅ افعل:
- استخدم `meta-llama/llama-3.2-3b-instruct:free` (الأكثر استقرارًا)
- جرب نماذج مختلفة إذا واجهت مشاكل
- تحقق من حسابك على OpenRouter بانتظام

### ❌ لا تفعل:
- لا تستخدم نماذج مدفوعة بدون رصيد
- لا تشارك مفتاح API علنًا
- لا تحاول إرسال طلبات كثيرة جدًا بسرعة

## 🐛 إذا استمرت المشكلة

### الخيار 1: استخدم Gemini بدلاً من OpenRouter

إذا استمرت مشاكل OpenRouter، يمكنك العودة لـ Gemini:

```env
# في .env
GEMINI_API_KEY=your_gemini_key
GEMINI_CHAT_MODEL=gemini-1.5-flash
```

وفي `app/stores/chat.ts`:
```typescript
const response = await $fetch("/api/chat/gemini", {
  // ...
});
```

### الخيار 2: جرب خدمة أخرى

خيارات أخرى مجانية:
- **Hugging Face Inference API**
- **Together AI** (نماذج مجانية)
- **Groq** (سريع جدًا ومجاني)

## 📝 الخلاصة

**المشكلة:** خطأ 429 من OpenRouter
**السبب:** النموذج المجاني مشغول أو مفتاح API غير صحيح
**الحل:** تغيير النموذج إلى `meta-llama/llama-3.2-3b-instruct:free`

---

**الآن، غيّر النموذج في `.env` وأعد تشغيل السيرفر!** 🚀
