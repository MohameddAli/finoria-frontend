# 🚀 خطوات تشغيل الشات بوت

## الخطوات المطلوبة الآن

### 1️⃣ أوقف السيرفر
في Terminal، اضغط:
```
Ctrl + C
```

### 2️⃣ تحقق من ملف `.env`
افتح ملف `.env` وتأكد من المحتوى:

```env
API_URL=https://www.aldorer.com/api
GEMINI_API_KEY=AIzaSyD68Gr85NC1A33pbb8qc8iWrM2veySMJnU4
GEMINI_CHAT_MODEL=gemini-1.5-flash
```

**مهم جدًا:**
- ✅ المفتاح يجب أن ينتهي بـ `U4`
- ✅ النموذج يجب أن يكون `gemini-1.5-flash`
- ❌ **ليس** `gemini-2.0-flash-exp`

### 3️⃣ أعد تشغيل السيرفر
في Terminal:

```bash
pnpm dev
```

انتظر حتى ترى:
```
✓ Nuxt Nitro server built in XXXms
➜ Local: http://localhost:3000/
```

### 4️⃣ اختبر الشات بوت

1. افتح المتصفح: http://localhost:3000
2. ابحث عن زر الشات العائم (أسفل يمين الشاشة)
3. انقر على الزر
4. اكتب: **"مرحبا"**
5. اضغط Enter أو زر الإرسال

**النتيجة المتوقعة:**
يجب أن يرد المساعد برسالة ترحيبية! 🎉

---

## إذا لم يعمل

### المشكلة: لا يزال يظهر خطأ 429
**السبب:** السيرفر لم يُعد تشغيله بعد
**الحل:** 
1. تأكد من إيقاف السيرفر تمامًا (Ctrl+C)
2. انتظر 5 ثوانٍ
3. شغّل مرة أخرى: `pnpm dev`

### المشكلة: الشات بوت لا يظهر
**الحل:**
1. تأكد من إضافة المكونات في الـ layout
2. افتح Developer Tools (F12)
3. تحقق من عدم وجود أخطاء في Console

### المشكلة: خطأ "API key not configured"
**الحل:**
1. تحقق من وجود ملف `.env` في جذر المشروع
2. تأكد من أن المفتاح صحيح وكامل
3. أعد تشغيل السيرفر

---

## نصائح مهمة

### ✅ افعل:
- أعد تشغيل السيرفر بعد أي تغيير في `.env`
- استخدم `gemini-1.5-flash` دائمًا
- احتفظ بمفتاح API آمنًا

### ❌ لا تفعل:
- لا تستخدم `gemini-2.0-flash-exp` (حدود صارمة)
- لا تشارك مفتاح API علنًا
- لا تنسى إعادة التشغيل بعد تعديل `.env`

---

## التحقق من النجاح

عند نجاح التشغيل، يجب أن ترى:

### في Terminal:
```
✓ Vite client built in XXXms
✓ Vite server built in XXXms
✓ Nuxt Nitro server built in XXXms
```

### في المتصفح:
- زر شات عائم أسفل يمين الشاشة
- عند النقر، تظهر نافذة محادثة
- عند إرسال رسالة، يرد المساعد

### في Console (عند إرسال رسالة):
```
[POST] "/api/chat/gemini": 200 OK
```

**ليس:**
```
[POST] "/api/chat/gemini": 500 Server Error
```

---

## الملفات المهمة

| الملف | الوصف |
|-------|-------|
| `.env` | إعدادات البيئة (المفتاح والنموذج) |
| `server/api/chat/gemini.post.ts` | API endpoint |
| `app/stores/chat.ts` | إدارة الحالة |
| `app/components/chatbot/` | مكونات الشات بوت |

---

## المساعدة

إذا واجهت مشاكل، راجع:
- `CHATBOT_SETUP.md` - دليل شامل
- `QUOTA_ISSUE_FIX.md` - حل مشكلة الحصة
- `GEMINI_MODELS.md` - معلومات عن النماذج

---

**الآن، أوقف السيرفر وأعد تشغيله!** 🔄
