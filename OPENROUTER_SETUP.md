# 🚀 دليل استخدام OpenRouter مع DeepSeek

## لماذا OpenRouter؟

### ✅ المميزات:
- **مجاني تمامًا** - بدون حدود صارمة
- **DeepSeek v3** - أحد أفضل النماذج المفتوحة
- **سريع جدًا** - استجابة فورية
- **موثوق** - بدون مشاكل quota
- **دعم ممتاز للعربية** - يفهم ويرد بالعربية بشكل ممتاز

### ❌ مشاكل Gemini التي تم حلها:
- ~~حدود صارمة (15 طلب/دقيقة)~~
- ~~أخطاء 429 Too Many Requests~~
- ~~نماذج تجريبية غير مستقرة~~

## 🔑 الحصول على API Key

### الخطوة 1: اذهب إلى OpenRouter
https://openrouter.ai/keys

### الخطوة 2: سجل دخول
- يمكنك التسجيل بـ Google أو GitHub
- مجاني تمامًا

### الخطوة 3: أنشئ API Key
1. انقر "Create Key"
2. أعطها اسم (مثل: "Roaya Banking")
3. انسخ المفتاح (يبدأ بـ `sk-or-v1-...`)

### الخطوة 4: أضف المفتاح في `.env`
```env
OPENROUTER_API_KEY=sk-or-v1-your-key-here
OPENROUTER_MODEL=deepseek/deepseek-chat-v3-0324:free
```

## 🎯 النماذج المجانية المتاحة

### 1. DeepSeek Chat v3 ⭐ (موصى به)
```env
OPENROUTER_MODEL=deepseek/deepseek-chat-v3-0324:free
```
- **السرعة:** سريع جدًا
- **الجودة:** ممتازة
- **العربية:** دعم ممتاز
- **الحد:** غير محدود (مجاني)

### 2. Meta Llama 3.2
```env
OPENROUTER_MODEL=meta-llama/llama-3.2-3b-instruct:free
```
- **السرعة:** سريع
- **الجودة:** جيدة
- **العربية:** جيد

### 3. Google Gemma 2
```env
OPENROUTER_MODEL=google/gemma-2-9b-it:free
```
- **السرعة:** متوسط
- **الجودة:** جيدة جدًا
- **العربية:** جيد

## 📊 مقارنة: OpenRouter vs Gemini

| الميزة | OpenRouter + DeepSeek | Gemini |
|--------|----------------------|--------|
| **السعر** | مجاني تمامًا | مجاني بحدود |
| **الحد** | غير محدود | 15 طلب/دقيقة |
| **الاستقرار** | ممتاز | متوسط |
| **السرعة** | سريع جدًا | سريع |
| **العربية** | ممتاز | ممتاز |
| **أخطاء 429** | لا يوجد | شائع |

## 🚀 التشغيل

### 1. تحديث `.env`
```env
OPENROUTER_API_KEY=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
OPENROUTER_MODEL=deepseek/deepseek-chat-v3-0324:free
```

### 2. إعادة تشغيل السيرفر
```bash
# أوقف السيرفر (Ctrl+C)
pnpm dev
```

### 3. اختبر الشات بوت
1. افتح http://localhost:3000
2. انقر على زر الشات
3. اكتب: "مرحبا"
4. يجب أن يرد بسرعة! 🎉

## 🔧 التخصيص

### تغيير النموذج
عدّل `OPENROUTER_MODEL` في `.env`:

```env
# للسرعة والجودة (موصى به)
OPENROUTER_MODEL=deepseek/deepseek-chat-v3-0324:free

# للتنوع
OPENROUTER_MODEL=meta-llama/llama-3.2-3b-instruct:free

# لجودة أعلى
OPENROUTER_MODEL=google/gemma-2-9b-it:free
```

### تغيير درجة الحرارة (Temperature)
في `server/api/chat/openrouter.post.ts`:

```typescript
temperature: 0.7, // 0.0 = دقيق، 1.0 = إبداعي
```

### تغيير الحد الأقصى للكلمات
```typescript
max_tokens: 1000, // عدد الكلمات في الرد
```

## 📝 الملفات المُحدّثة

- ✅ `server/api/chat/openrouter.post.ts` - API endpoint جديد
- ✅ `app/stores/chat.ts` - تحديث لاستخدام OpenRouter
- ✅ `.env` - إضافة مفاتيح OpenRouter
- ✅ `.env.example` - مثال محدّث

## 🎯 الميزات الإضافية

### 1. بدون حدود
لا توجد حدود صارمة على عدد الطلبات

### 2. نماذج متعددة
يمكنك التبديل بين النماذج بسهولة

### 3. تتبع الاستخدام
راقب استخدامك على: https://openrouter.ai/activity

### 4. دعم فني
مجتمع نشط على Discord: https://discord.gg/openrouter

## ⚠️ ملاحظات مهمة

### ✅ افعل:
- استخدم `deepseek/deepseek-chat-v3-0324:free` للأداء الأفضل
- احتفظ بمفتاح API آمنًا
- راقب استخدامك على OpenRouter

### ❌ لا تفعل:
- لا تشارك مفتاح API علنًا
- لا تستخدم نماذج مدفوعة بدون تفعيل الدفع
- لا تنسى إعادة التشغيل بعد تعديل `.env`

## 🐛 استكشاف الأخطاء

### المشكلة: "API key not configured"
**الحل:**
1. تحقق من وجود `OPENROUTER_API_KEY` في `.env`
2. تأكد من أن المفتاح يبدأ بـ `sk-or-v1-`
3. أعد تشغيل السيرفر

### المشكلة: "Model not found"
**الحل:**
1. تحقق من اسم النموذج في `.env`
2. تأكد من إضافة `:free` في نهاية اسم النموذج
3. راجع القائمة الكاملة: https://openrouter.ai/models?order=newest&supported_parameters=tools

### المشكلة: ردود بطيئة
**الحل:**
1. جرب نموذج أسرع (DeepSeek)
2. قلل `max_tokens` في الكود
3. تحقق من اتصال الإنترنت

## 📚 موارد إضافية

- **الموقع الرسمي:** https://openrouter.ai
- **التوثيق:** https://openrouter.ai/docs
- **النماذج المتاحة:** https://openrouter.ai/models
- **الأسعار:** https://openrouter.ai/docs/limits

## 🎊 الخلاصة

**OpenRouter + DeepSeek = حل مثالي!**

- ✅ مجاني تمامًا
- ✅ بدون حدود صارمة
- ✅ سريع وموثوق
- ✅ دعم ممتاز للعربية
- ✅ سهل الاستخدام

---

**الآن، أعد تشغيل السيرفر واستمتع بشات بوت بدون مشاكل!** 🚀
