# دليل تشغيل الشات بوت - منصة رؤية المصرفية

## ✅ الحالة الحالية
جميع المكونات جاهزة ومثبتة! تم إكمال Task 8 بنجاح.

## ⚠️ تحديث مهم
- **النموذج الموصى به:** `gemini-1.5-flash` (سريع ومستقر مع حدود سخية)
- تجنب استخدام `gemini-2.0-flash-exp` في الإنتاج (حدود صارمة جدًا)
- يمكنك تغيير النموذج عبر متغير `GEMINI_CHAT_MODEL` في ملف `.env`
- النموذج الافتراضي هو `gemini-1.5-flash` إذا لم تحدد نموذجًا

## 🚀 خطوات التشغيل

### 1. إضافة مفتاح Gemini API

أنشئ ملف `.env` في جذر المشروع وأضف:

```env
GEMINI_API_KEY=your_api_key_here
GEMINI_CHAT_MODEL=gemini-1.5-flash
```

**للحصول على مفتاح مجاني:**
1. اذهب إلى: https://makersuite.google.com/app/apikey
2. سجل دخول بحساب Google
3. انقر على "Create API Key"
4. انسخ المفتاح وضعه في ملف `.env`

**اختيار النموذج:**
- `gemini-1.5-flash` - سريع ومستقر (موصى به) ✅
- `gemini-1.5-pro` - للمهام المعقدة
- `gemini-2.0-flash-exp` - تجريبي (حدود صارمة، غير موصى به)

### 2. إضافة الشات بوت للتطبيق

افتح ملف `app/layouts/dashboard.vue` وأضف المكونات:

```vue
<template>
  <v-app>
    <!-- المحتوى الحالي للـ layout -->
    
    <!-- إضافة الشات بوت -->
    <AIChatbotFAB />
    <AIChatbotDialog />
  </v-app>
</template>

<script setup lang="ts">
// المكونات ستُستورد تلقائيًا من Nuxt
</script>
```

### 3. تشغيل المشروع

```bash
npm run dev
```

## 🎯 ما يفعله الشات بوت

الشات بوت الآن مُخصص لمنصة رؤية المصرفية ويمكنه:

### صفحة دعم المشاريع
- شرح كيفية البحث عن المشاريع
- توضيح طريقة دعم المشاريع
- شرح نظام الفلترة والوسوم
- مساعدة في فهم تفاصيل المشروع

### صفحة ماكينات الصراف الآلي
- إرشاد المستخدم لإيجاد أقرب ماكينة
- شرح الفلاتر المتاحة
- توضيح كيفية استخدام خاصية "قريب مني"
- مساعدة في فهم حالة الماكينات

### صفحة نقاط البيع
- شرح الفرق بين POS و NPG
- مساعدة في إيجاد متاجر قريبة
- توضيح كيفية تصدير البيانات
- شرح أنواع البطاقات المقبولة

## 📁 الملفات المُنشأة

```
app/
├── components/
│   └── chatbot/
│       ├── AIChatbotFAB.vue       ✅ زر الشات العائم
│       └── AIChatbotDialog.vue    ✅ نافذة المحادثة
├── stores/
│   └── chat.ts                    ✅ إدارة حالة المحادثة
├── assets/
│   └── css/
│       └── chatbot.css            ✅ أنماط الشات بوت
└── server/
    └── api/
        └── chat/
            └── gemini.post.ts     ✅ API endpoint (مُحدّث)
```

## 🎨 التخصيص

### تغيير نموذج Gemini
عدّل ملف `.env`:

```env
# للاستقرار والسرعة (موصى به) ✅
GEMINI_CHAT_MODEL=gemini-1.5-flash

# للدقة والتحليل العميق
GEMINI_CHAT_MODEL=gemini-1.5-pro

# تجريبي (حدود صارمة، غير موصى به)
GEMINI_CHAT_MODEL=gemini-2.0-flash-exp
```

راجع ملف `GEMINI_MODELS.md` لمزيد من التفاصيل عن كل نموذج.

### تغيير الألوان
عدّل ملف `app/assets/css/chatbot.css`:

```css
.chatbot-header {
  background: rgb(var(--v-theme-primary)); /* لون الهيدر */
}

.user-message .message-content {
  background: rgb(var(--v-theme-primary)); /* لون رسائل المستخدم */
}
```

### تغيير موضع الزر
في `chatbot.css`:

```css
[dir="ltr"] .chatbot-fab {
  right: 24px;  /* المسافة من اليمين */
  bottom: 24px; /* المسافة من الأسفل */
}
```

### تخصيص الرسائل الترحيبية
عدّل `app/stores/chat.ts`:

```typescript
const welcomeMessage: Message = {
  id: generateId(),
  role: 'assistant',
  content: 'مرحباً بك في منصة رؤية! 👋 كيف يمكنني مساعدتك اليوم؟',
  timestamp: new Date()
};
```

## 🔧 استكشاف الأخطاء

### المشكلة: خطأ "429 Too Many Requests" أو "Quota exceeded"
**السبب:**
- النموذج `gemini-2.0-flash-exp` له حدود استخدام صارمة جدًا (تجريبي)
- قد تكون استنفذت الحصة المجانية

**الحل:**
1. استخدم `gemini-1.5-flash` بدلاً من `gemini-2.0-flash-exp`:
   ```env
   GEMINI_CHAT_MODEL=gemini-1.5-flash
   ```
2. أو احصل على مفتاح API جديد من https://makersuite.google.com/app/apikey
3. انتظر 1-2 دقيقة قبل المحاولة مرة أخرى

**حدود الاستخدام المجاني:**
- `gemini-1.5-flash`: 15 طلب/دقيقة ✅ (موصى به)
- `gemini-1.5-pro`: 2 طلب/دقيقة
- `gemini-2.0-flash-exp`: محدود جدًا (تجريبي)

### المشكلة: خطأ "models/gemini-pro is not found"
**الحل:**
- تم تحديث الكود ليستخدم `gemini-1.5-flash` بدلاً من `gemini-pro`
- إذا واجهت هذا الخطأ، تأكد من أن ملف `server/api/chat/gemini.post.ts` يستخدم النموذج الصحيح

### المشكلة: الشات بوت لا يظهر
**الحل:**
1. تأكد من إضافة المكونات في الـ layout
2. تحقق من أن الـ CSS مُستورد في `global.css`
3. افتح Developer Tools وتحقق من عدم وجود أخطاء

### المشكلة: لا يرد على الرسائل
**الحل:**
1. تحقق من وجود `GEMINI_API_KEY` في `.env`
2. تأكد من صحة المفتاح (احصل على مفتاح جديد من https://makersuite.google.com/app/apikey)
3. افتح Network tab وتحقق من استجابة `/api/chat/gemini`
4. تحقق من console للتأكد من عدم وجود أخطاء

### المشكلة: الردود بالإنجليزية
**الحل:**
- تأكد من أن `locale` في الـ store مضبوط على `'ar'`

## 📱 الميزات المتاحة

- ✅ دعم اللغة العربية والإنجليزية
- ✅ واجهة RTL كاملة
- ✅ تصميم متجاوب (موبايل، تابلت، ديسكتوب)
- ✅ حفظ سجل المحادثة
- ✅ رسوم متحركة سلسة
- ✅ دعم الثيمات الفاتحة والداكنة
- ✅ إمكانية مسح المحادثة
- ✅ عرض وقت كل رسالة

## 🎓 أمثلة على الأسئلة

جرب هذه الأسئلة مع الشات بوت:

```
- كيف أدعم مشروع خيري؟
- أين أقرب ماكينة صراف آلي؟
- ما الفرق بين POS و NPG؟
- كيف أبحث عن مشروع في مدينة معينة؟
- كيف أصدر قائمة نقاط البيع؟
```

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من هذا الملف أولاً
2. راجع console في المتصفح
3. تحقق من logs الـ server

---

**ملاحظة:** الشات بوت يعمل بالكامل من مشروع Nuxt بدون الحاجة لـ backend منفصل! 🎉
