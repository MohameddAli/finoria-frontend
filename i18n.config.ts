export default {
  legacy: false,
  // Set Arabic as default locale
  locale: 'ar',
  // Fallback to English if a key is missing in Arabic
  fallbackLocale: 'en',
  globalInjection: true,
  warnHtmlMessage: false,
}


