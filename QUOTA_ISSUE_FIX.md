# إصلاح مشكلة Quota Exceeded

## المشكلة 🔴

```
[429 Too Many Requests] You exceeded your current quota
Quota exceeded for metric: generativelanguage.googleapis.com/generate_content_free_tier_requests
model: gemini-2.0-flash-exp
```

## السبب 🤔

النموذج `gemini-2.0-flash-exp` هو نموذج **تجريبي** له حدود استخدام صارمة جدًا:
- حد منخفض جدًا للطلبات المجانية
- غير مناسب للاستخدام المكثف
- قد يتوقف أو يتغير في أي وقت

## الحل ✅

### 1. تغيير النموذج إلى `gemini-1.5-flash`

في ملف `.env`:

```env
# قبل (يسبب المشكلة)
GEMINI_CHAT_MODEL=gemini-2.0-flash-exp

# بعد (الحل)
GEMINI_CHAT_MODEL=gemini-1.5-flash
```

### 2. تحديث مفتاح API (إذا لزم الأمر)

إذا كان المفتاح القديم استنفذ حصته:

```env
GEMINI_API_KEY=your_new_api_key_here
```

احصل على مفتاح جديد من: https://makersuite.google.com/app/apikey

### 3. إعادة تشغيل المشروع

```bash
# أوقف المشروع (Ctrl+C)
# ثم شغّله مرة أخرى
pnpm dev
```

## مقارنة النماذج 📊

| النموذج | الطلبات/دقيقة | التوصية |
|---------|---------------|----------|
| `gemini-1.5-flash` | 15 | ✅ موصى به |
| `gemini-1.5-pro` | 2 | ⚠️ للمهام المعقدة فقط |
| `gemini-2.0-flash-exp` | محدود جدًا | ❌ غير موصى به |

## لماذا `gemini-1.5-flash` هو الأفضل؟

### ✅ المميزات:
- **15 طلب/دقيقة** - كافٍ للاستخدام العادي
- **1 مليون token/دقيقة** - حد سخي جدًا
- **سريع** - استجابة فورية
- **مستقر** - موثوق للإنتاج
- **مجاني** - بدون تكلفة

### ❌ عيوب النماذج الأخرى:

**gemini-2.0-flash-exp:**
- حدود صارمة جدًا
- تجريبي وغير مستقر
- قد يُزال في أي وقت

**gemini-1.5-pro:**
- 2 طلب/دقيقة فقط
- بطيء نسبيًا
- مناسب فقط للمهام المعقدة

## الخطوات التفصيلية 📝

### الخطوة 1: تحديث `.env`

```bash
# افتح ملف .env
nano .env

# أو في Windows
notepad .env
```

### الخطوة 2: عدّل المحتوى

```env
API_URL=https://www.aldorer.com/api
GEMINI_API_KEY=AIzaSyD68Gr85NC1A33pbb8qc8iWrM2veySMJnU4
GEMINI_CHAT_MODEL=gemini-1.5-flash
```

### الخطوة 3: احفظ وأعد التشغيل

```bash
# احفظ الملف (Ctrl+S)
# أوقف المشروع (Ctrl+C)
# شغّل مرة أخرى
pnpm dev
```

### الخطوة 4: اختبر الشات بوت

1. افتح المتصفح: http://localhost:3000
2. انقر على زر الشات
3. اكتب: "مرحبا"
4. يجب أن يرد بنجاح! 🎉

## إذا استمرت المشكلة 🔧

### الحل 1: انتظر دقيقة

```bash
# انتظر 1-2 دقيقة ثم حاول مرة أخرى
```

### الحل 2: احصل على مفتاح جديد

1. اذهب إلى: https://makersuite.google.com/app/apikey
2. انقر "Create API Key"
3. انسخ المفتاح الجديد
4. ضعه في `.env`

### الحل 3: تحقق من الحصة

1. اذهب إلى: https://ai.dev/usage?tab=rate-limit
2. تحقق من استخدامك الحالي
3. انتظر حتى تتجدد الحصة

## نصائح للاستخدام الأمثل 💡

### ✅ افعل:
- استخدم `gemini-1.5-flash` دائمًا
- احتفظ بمفتاح API آمنًا
- راقب استخدامك على https://ai.dev/usage

### ❌ لا تفعل:
- لا تستخدم `gemini-2.0-flash-exp` في الإنتاج
- لا تشارك مفتاح API علنًا
- لا ترسل طلبات كثيرة جدًا في وقت قصير

## الخلاصة 📌

**المشكلة:** استخدام نموذج تجريبي بحدود صارمة
**الحل:** التبديل إلى `gemini-1.5-flash`
**النتيجة:** شات بوت يعمل بسلاسة! ✅

---

**تم الإصلاح بنجاح!** 🎉

الآن يمكنك استخدام الشات بوت بدون مشاكل.
